import { Injectable } from '@angular/core';

/**
 * Servicio placeholder generado por Angular CLI.
 * La lógica real de autenticación se encuentra en {@link AuthService}.
 *
 * @see AuthService
 */
@Injectable({ providedIn: 'root' })
export class Auth {}
